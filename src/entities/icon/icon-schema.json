{
    "$id": "Icon",
    "type": "object",

    "properties": {
        "icon_id": {
            "type": "integer"
        },

        "file_name": {
            "type": "string",
            "minLength": 5,
            "maxLength": 20
        }
    },

    "required": ["icon_id", "file_name"]
}
