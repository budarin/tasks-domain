{
    "$id": "Icon",
    "type": "object",

    "properties": {
        "icon_id": {
            "type": "integer",
            "minimum": 1
        },

        "file_name": {
            "type": "string",
            "minLength": 5,
            "maxLength": 128
        }
    },

    "required": ["icon_id", "file_name"]
}
