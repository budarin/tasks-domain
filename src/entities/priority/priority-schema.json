{
    "$id": "https://budarin/MyTasks/taskPriority.json",

    "type": "object",

    "properties": {
        "priority_id": {
            "type": "integer",
            "minimum": 1
        },

        "priority_name": {
            "type": "string",
            "enum": ["низкий", "нормальный", "повышенный"]
        }
    },

    "required": ["priority_id", "priority_name"]
}
