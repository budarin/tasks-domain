{
    "$id": "https://budarin/MyTasks/newTask.json",
    "type": "object",

    "properties": {
        "title": {
            "type": "string"
        },

        "priority_id": {
            "type": "integer"
        },

        "category_id": {
            "type": "integer",
            "nullable": true
        },

        "description": {
            "type": "string",
            "nullable": true
        },

        "expire_date_time": {
            "type": "string",
            "format": "iso-date-date",
            "nullable": true
        },

        "deleted": {
            "type": "boolean",
            "nullable": true,
            "default": false
        },

        "completed": {
            "type": "boolean",
            "nullable": true,
            "default": false
        }
    },

    "required": ["title", "priority_id"]
}
