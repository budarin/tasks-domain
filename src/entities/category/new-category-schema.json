{
    "$id": "newTaskCategory",
    "type": "object",
    "properties": {
        "icon_id": {
            "type": "integer",
            "minimum": 1
        },

        "category_name": {
            "type": "string",
            "minLength": 3,
            "maxLength": 20
        }
    },
    "required": ["category_id", "icon_id", "category_name"],
    "additionalProperties": false
}
